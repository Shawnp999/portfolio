import './Footer.css';

const Footer = () => {
    //const currentYear = new Date().getFullYear();

    return (
        <footer className="footer">
            <p> Footer text </p>
        </footer>
    );
};

export default Footer;